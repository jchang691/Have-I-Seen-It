$.jGrowl("<%= @movie.name %> has been Un-Seen");
var str = "<%= escape_javascript(render @movie) %>";
str = str.replace(/<li id="movie_\w+">/, "");
str = str.replace(/<li>/, "");
$("li#movie_<%= @movie.id %>").html(str);
